PORT=3000
# The server uses Application Default Credentials (ADC) by default.
# Ensure the environment has access to BigQuery.
# If running locally, you might need:
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/key.json
